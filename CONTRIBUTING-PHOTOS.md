# 📷 贡献你自己拍的杯子照片

这个图鉴的实拍照片来自收藏者本人。**只上传你自己拍摄的、你自己拥有的杯子。**
照片著作权仍归你，你只是允许本项目展示它。

## 怎么贡献

1. **拍照**：白墙或浅色桌面、自然光，杯子正面朝前，尽量填满画面。
   横竖都行，长边 800px 以上即可（脚本会压到 520px）。

2. **命名**：文件名必须是该杯款的 `id`，扩展名 `.jpg` / `.jpeg` / `.png` / `.webp`。
   id 在 `js/data.js` 里，也可以在网站详情页地址栏看到。

   ```
   img/gi-shanghai.jpg
   img/bt-california-orn.jpg
   ```

   同一杯款多角度：加 `-2`、`-3` 后缀，第一张为主图。

3. **登记署名**（可选但推荐）：在 `img/CREDITS.tsv` 追加一行，用 Tab 分隔：

   ```
   gi-shanghai	@你的GitHub用户名	CC BY 4.0
   ```

   不写默认视为 CC BY 4.0，署名为提交 PR 的账号。

4. **生成映射并提 PR**：

   ```bash
   python3 tools/build_photos.py     # 扫描 img/，压缩、写入 js/photos-index.js
   cd test && npm test               # 确认没弄坏什么
   ```

   然后提交 PR，标题写「photos: 城市名 xN」。

## 会被退回的情况

- 从别的网站、eBay листинг、社交媒体保存的图片——**不管有没有水印**。
  别人拍的照片著作权归拍摄者，这个项目不收。
- 带他人水印、商标叠加、或明显是产品宣传图的。
- 杯子不是你本人拥有的（借拍需征得所有者同意并注明）。
- 含人脸、地址、快递单等个人信息的。

## 为什么这样做

站内不抓取、不转存任何第三方网站的实拍图（详见 [DATA-SOURCES.md](DATA-SOURCES.md)）。
收藏者各自拍自己的杯子，是唯一能攒出一套**权利清晰**的图库的办法，
而且真实杯况、真实光线，比产品宣传图更有参考价值。

## 本地预览你自己的照片（不提 PR）

不想公开分享的话，直接在网站详情页点「📷 上传实拍照片」即可——
照片只存在你自己的浏览器里，导出备份时会一起带走，分享链接里不包含它。
